(include "./widgets.yuck")

(defwindow bar
  :monitor 0
  :geometry (geometry
              :x "0%"
              :y "0%"
              :height "100%"
              :anchor "center left")
  :windowtype "dock"
  :reserve (struts :distance "72px" :side "left")
  (bar))

